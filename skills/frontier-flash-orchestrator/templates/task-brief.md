# Task Brief: [Bundle / Phase Name]

> **Instructions for Implementation Worker**:
> You are the implementation worker. Your role is to write clean, complete code and verify it with automated tests. Do not alter architecture or modify files outside the in-scope list without explicit instructions.

## 1. Scope & Target Files
- **Files to Create**:
  - `path/to/new_file.ext`
- **Files to Modify**:
  - `path/to/existing_file.ext`
- **Files NOT to Touch**:
  - Configuration files, credentials, or unrelated project files.

## 2. Requirements & Behavior
1. Implement [exact function/class signature].
2. Ensure proper error handling for edge cases.
3. Write/update unit tests verifying the expected behavior.

## 3. Mandatory Verification Command
Run this command in the terminal to verify your work before concluding:
```bash
[e.g. npm test or python -m unittest path/to/test.py]
```

## 4. Expected Output
When done, summarize:
1. Files changed/created.
2. Verification command output (must show passing tests).
3. Any edge cases or findings discovered during implementation.
